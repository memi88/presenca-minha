{
  "name": "presenca-monorepo",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "dev:presenca": "pnpm --filter presenca dev",
    "dev:cuida": "pnpm --filter cuida dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@supabase/supabase-js": "latest"
  }
}
