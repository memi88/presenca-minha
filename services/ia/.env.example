# Chave compartilhada entre este serviço e o backend do Next.js (Server
# Actions apenas — nunca o client-side). Gere com:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
# Configure o mesmo valor aqui (local) e no Render/Railway (produção). Não é
# service_role nem senha de banco — é uma chave de serviço-a-serviço criada
# especificamente pra este microsserviço.
IA_API_KEY=
